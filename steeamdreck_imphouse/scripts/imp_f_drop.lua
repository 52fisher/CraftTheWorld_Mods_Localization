pos = getEventPosition();
world = World.new();
world:spawn("imp", pos.x, pos.y);
world:spawn("imp", pos.x, pos.y);
world:spawn("imp", pos.x, pos.y);
world:spawn("imp", pos.x, pos.y);
world:spawn("imp", pos.x, pos.y);
--steeamdreck was here